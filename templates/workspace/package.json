{
  "private": true,
  "name": "{{ name }}",
  "version": "{{ version }}",
  "engines": {
    "node": ">=10.3.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "preinstall": "autod",
    "commit": "git add . && npx git-cz",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "lint": "esnext lint packages",
    "test": "esnext test"
  }
}
